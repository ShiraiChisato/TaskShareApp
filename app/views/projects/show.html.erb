  <div class="title">
    <p>Project Name:<%= @project.name %></p>
  </div>
  <div class="field">
    <p>What did you do?</p>
    <div class="title">
    </div>
  </div>
  <div class = "side">
    <% @members.each do |member| %>
      <% if member == user.hosts.name %>
      <span><%= member.name %></span>
      <% else %>
      <%= member.name %>
    <% end %>
  </div>

